# Triviaverse Web (Vite + React + TS)

## Quickstart

```bash
npm install
cp .env.example .env.local
npm run dev
```

Fill `.env.local` with your Firebase Web App config (`VITE_FIREBASE_*`). See the repo root `README.md` for Firestore schema and seed examples.

## Build

```bash
npm run build
```
